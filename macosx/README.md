# SmartTicker for macOS

This tracked placeholder is reserved for future macOS application-bundle, signing, notarization, and DMG packaging resources.

Shared ticker, extraction, feed, and persistence logic will be reused from the portable application layers. The Windows MVP is implemented first.
